
public class Main {

	public static void main(String[] args) {


		/* TAMANHO DA LISTA 
		size();
		
		INSERIR ELEMENTO NA LISTA:
		add(obj), add(int, obj)
		
		REMOVER ELEMENTOS DA LISTA:
		remove(obj), remove(int), removeIf(Predicate)
		
		ENCONTRAR POSICAO DO ELEMENTO: 
		indexOf(obj), lastIndexOf(obj)
		
		FILTRAR LISTA COM BASE EM PREDICADO:
		lis<Integer> result = list.stream().filter(x -> x > 4).collect(Collectors.toList());
		
		ENCONTRAR PRIMEIRA OCORRENCIA COM BASE EM PREDICADO:
		Integer  result = list.stream().filter(x -> x > 4).findFirst().orElse(null);


		 */
	}

}
